<h1>Todos los Productos</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <%= product.name %>
      <br>
      <%= product.price %>
      <%= button_to "Agregar al Carrito", line_items_path(product_id: product.id) %>
    </li>
  <% end %>
</ul>
